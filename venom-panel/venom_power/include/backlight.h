#ifndef BACKLIGHT_H
#define BACKLIGHT_H

#include <glib.h>

// ═══════════════════════════════════════════════════════════════════════════
// 💡 التحكم بالسطوع
// ═══════════════════════════════════════════════════════════════════════════

/**
 * الحصول على مستوى السطوع الحالي
 * @return مستوى السطوع أو -1 في حالة الفشل
 */
gint backlight_get_brightness(void);

/**
 * الحصول على أقصى مستوى سطوع
 * @return أقصى مستوى أو -1 في حالة الفشل
 */
gint backlight_get_max_brightness(void);

/**
 * تعيين مستوى السطوع
 * @param level المستوى المطلوب
 * @return TRUE في حالة النجاح
 */
gboolean backlight_set_brightness(gint level);

/**
 * تعتيم الشاشة (تقليل السطوع إلى 30%)
 * @param dim TRUE للتعتيم، FALSE للاستعادة
 */
void backlight_dim_screen(gboolean dim);

/**
 * إطفاء/تشغيل الشاشة عبر DPMS
 * @param blank TRUE للإطفاء، FALSE للتشغيل
 */
void backlight_blank_screen(gboolean blank);

#endif // BACKLIGHT_H
